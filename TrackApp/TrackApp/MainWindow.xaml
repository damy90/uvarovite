<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Name="Video_Composer" x:Class="TrackApp.MainWindow"
        Title="Video Composer" Height="354" Width="593" Loaded="MainWindowLoaded"
        ResizeMode="NoResize">
    <Window.Resources>
            <!-- This style is used for buttons, to remove the WPF default 'animated' mouse over effect -->
            <Style x:Key="MyButtonStyle" TargetType="Button">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Name="border" 
                            BorderThickness="0"
                            Padding="0"
                            CornerRadius="0" 
                            Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
    </Window.Resources>
        <Grid Background="LightBlue" Margin="0,0,0,-7">
        <Button x:Name="btnOK"
        		Style="{StaticResource MyButtonStyle}" 
        		HorizontalAlignment="Left"
        		Margin="464,255,0,20" 
        		Width="95"
        		RenderTransformOrigin="0.5,0.5"
        		MouseEnter="btnLoadGpx_Enter"
        		MouseLeave="btnLoadGpx_Leave" Click="btnOKClick"
                Content="----&gt;"
                >
            <!--картинка-->
        </Button>
        <Grid x:Name="gridSelections" HorizontalAlignment="Left" Height="295" Margin="-1,21,0,0" VerticalAlignment="Top" Width="172">
            <Button x:Name="btnLoadVideo"
        		Style="{StaticResource MyButtonStyle}" 
        		HorizontalAlignment="Left"
        		Margin="10,101,0,106" 
        		Width="153" 
        		RenderTransformOrigin="0.267,-1.364"
        		Height="88" BorderThickness="0"
        		Click="btnLoadClick" 
        		MouseEnter="btnLoadVideo_Enter"
        		MouseLeave="btnLoadVideo_Leave">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/iconVideo_leave.png"/>
                </Button.Background>
            </Button>
            <Button x:Name="btnLoadGpx"
            	Style="{StaticResource MyButtonStyle}" 
            	HorizontalAlignment="Left"
            	Margin="10,0,0,199" 
            	Width="153"
            	RenderTransformOrigin="0.506,0.45"
            	MouseEnter="btnLoadGpx_Enter"
            	MouseLeave="btnLoadGpx_Leave" Click="btnLoadGPXClick"
                >
                <Button.Background>
                    <ImageBrush ImageSource="Resources/iconGPX_leave.png"/>
                </Button.Background>
            </Button>
            <Button x:Name="btnAdjustSettings"
            	Style="{StaticResource MyButtonStyle}" 
            	HorizontalAlignment="Left"
            	Margin="10,194,0,5" 
            	Width="153"
            	RenderTransformOrigin="0.506,0.45"
            	MouseEnter="btnAdjustSettings_Enter"
            	MouseLeave="btnAdjustSettings_Leave" Click="btnAdjustSettings_Click"
                >
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/iconSettings_Leave.png"/>
                </Button.Background>
            </Button>
        </Grid>
        <Grid x:Name="gridPreview" HorizontalAlignment="Left" Height="246" Margin="171,10,0,0" VerticalAlignment="Top" Width="388">
            <Image x:Name="imgPreview" Source="Resources/imgPreview.jpg" HorizontalAlignment="Left" Height="230" Margin="6,10,0,0" VerticalAlignment="Top" Width="367"/>
            <Image x:Name="imgTrack" HorizontalAlignment="Left" Height="50" Margin="290,194,0,0" VerticalAlignment="Top" Width="83" Source="Resources/imgTrack.png"/>
            <TextBlock x:Name="tbSpeed" HorizontalAlignment="Left" Margin="6,220,0,0" TextWrapping="Wrap" Text=" 50 km/h" VerticalAlignment="Top" Background="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" Height="20" Width="49"/>
            <TextBlock x:Name="tbDistance" HorizontalAlignment="Left" Margin="164,220,0,0" TextWrapping="Wrap" Text="20.5 km" VerticalAlignment="Top" Background="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" Height="21" Width="47"/>
        </Grid>
    </Grid>
    
</Window>
